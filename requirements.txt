numpy>=1.7.0
imutils
opencv-python>=4.1.0.25
scipy
mac-tag
psd-tools3
lpips
PyMuPDF
scikit-learn
scikit-image

# human_filter_unique.py dependencies
torch>=2.0.0
torchvision
transformers>=4.30.0
ultralytics>=8.0.0
tqdm
pillow
scikit-learn-extra

# Optional: additional detectors
easyocr                  # GPU text detection (recommended)
# paddlepaddle paddleocr # Alternative text detection (version issues)
# Moondream uses transformers (already installed) - no extra package needed
